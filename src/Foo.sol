// SPDX-License-Identifier: UNLICENSED
pragma solidity 0.8.34;

/// @notice Placeholder kept so the foundry-template `tests/Foo.t.sol` compiles. Safe to delete
///         along with `tests/Foo.t.sol` once the real test suite is the only thing in the tree.
contract Foo {
    function echo(uint256 value) external pure returns (uint256) {
        return value;
    }
}
