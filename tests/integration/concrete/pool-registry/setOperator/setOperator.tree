SetOperator_PoolRegistry_Integration_Concrete_Test
├── when caller not owner
│  └── it should revert
└── when caller owner
   ├── when new operator zero
   │  ├── it should set the operator to zero
   │  └── it should emit OperatorSet
   └── when new operator not zero
      ├── it should set the operator
      └── it should emit OperatorSet
