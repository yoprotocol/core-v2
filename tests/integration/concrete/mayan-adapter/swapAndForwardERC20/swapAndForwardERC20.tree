SwapAndForwardERC20_MayanAdapter_Integration_Concrete_Test
├── when amountIn zero
│  └── it should revert with InvalidAmount
└── when amountIn non zero
   ├── when order token not middleToken
   │  └── it should revert with ProtocolDataMismatch
   ├── when trader not vault
   │  └── it should revert with TraderNotVault
   ├── when route not allowed
   │  └── it should revert with RouteNotAllowed
   ├── when swap pair disallowed
   │  └── it should revert with PairNotAllowed
   ├── when minMiddleAmount below oracle floor
   │  └── it should revert with SlippageTooLow
   └── when valid and within slippage
      ├── it should pull amountIn from the vault
      ├── it should forward swap params and mayanData to the forwarder
      ├── it should reset forwarder allowance to zero
      └── it should return amountIn
